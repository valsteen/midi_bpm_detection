# Bitwig Extension Third-Party Notices

The packaged Bitwig extension contains these resolved runtime dependencies:

- Kotlin Standard Library `2.4.0`, licensed under Apache-2.0;
- JetBrains Annotations `13.0`, licensed under Apache-2.0.

The full Apache License 2.0 text is included as `third-party-licenses/Apache-2.0.txt`. Bitwig API classes are compile-only
and package verification rejects an archive that bundles them.

This notice describes the resolved extension runtime graph and is not legal advice.
