[package]
name = "sync"
version = "0.1.0"
edition = "2024"

[dependencies]
atomic-traits = "0.4.0"
serde = { version = "1.0.228" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
parking_lot = { version = "0.12.5" }

[lints]
workspace = true
